# Soft Vector Quantization via Entropy Regularized Neural Activation Patterns

Use the `heatmaps.ipynb` notebook to recreate fig 1.

Run `vq_convergence.py` to recreate fig 3.

Use `forward_pass_time.ipynb` and `kernel_alignment.ipynb` to recreate figs 12a and 12b, respectively.